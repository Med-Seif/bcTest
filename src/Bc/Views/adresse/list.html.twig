{% extends 'layout.html.twig' %}
{% block title %}
    Liste des adresses
{% endblock %} 
{% block content %}
    <table class="pure-table">
        <thead>
            <tr>
                <th>Adresse</th>
                <th>Actions</th>
            </tr>
        </thead>
        {% for item in adresses %}
            <tr>
                <td>{{ item.libelle }}</td>
                <td><a class="pure-button" href="{{ base_url }}/adresse/edit?id={{ item.id }}">Éditer</a></td>
            </tr>
        {% endfor %}
    </table>
{% endblock %}